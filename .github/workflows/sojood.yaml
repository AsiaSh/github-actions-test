name: sojood-workflow
on: [push]
jobs:
  read-hello-file:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with:
          node-version: '20.x'
    - run: node sayHello.js
